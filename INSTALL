
This is the Perl distribution Net-IP-Match.

## Installation

Net-IP-Match installation is straightforward.
If your CPAN shell is set up, you should just be able to do

    % cpan Net::IP::Match

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

## Documentation

Net-IP-Match documentation is available as in POD.
So you can do:

    % perldoc Net::IP::Match

to read the documentation with your favorite pager.
